name: Auto Deploy VPS

on:
  workflow_run:
    workflows:
      - Publish Docker Image
    types:
      - completed
  workflow_dispatch:

permissions:
  contents: read
  packages: read

concurrency:
  group: vps-auto-deploy
  cancel-in-progress: true

jobs:
  deploy:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.event == 'push' &&
        github.event.workflow_run.head_branch == 'master'
      )
    runs-on: ubuntu-latest
    steps:
      - name: Validate deploy secrets
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          set -euo pipefail
          [ -n "${VPS_HOST:-}" ] || { echo "Missing secret: VPS_HOST" >&2; exit 1; }
          [ -n "${VPS_USER:-}" ] || { echo "Missing secret: VPS_USER" >&2; exit 1; }
          [ -n "${VPS_SSH_KEY:-}" ] || { echo "Missing secret: VPS_SSH_KEY" >&2; exit 1; }

      - name: Configure SSH
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_PORT: ${{ secrets.VPS_PORT }}
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
          VPS_KNOWN_HOSTS: ${{ secrets.VPS_KNOWN_HOSTS }}
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          printf "%s\n" "$VPS_SSH_KEY" | tr -d '\r' > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keygen -y -f ~/.ssh/id_ed25519 > /dev/null

          if [ -n "${VPS_KNOWN_HOSTS:-}" ]; then
            printf "%s\n" "$VPS_KNOWN_HOSTS" > ~/.ssh/known_hosts
          else
            PORT="${VPS_PORT:-22}"
            ssh-keyscan -p "$PORT" "$VPS_HOST" > ~/.ssh/known_hosts
          fi

          chmod 600 ~/.ssh/known_hosts

      - name: Deploy latest image on VPS
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_PORT: ${{ secrets.VPS_PORT }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_APP_PATH: ${{ vars.VPS_APP_PATH }}
          GHCR_USER: ${{ github.actor }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          PORT="${VPS_PORT:-22}"
          APP_PATH="${VPS_APP_PATH:-/opt/jetllm}"
          [ -n "${GHCR_TOKEN:-}" ] || { echo "Missing GitHub token for GHCR pull" >&2; exit 1; }

          ssh \
            -i ~/.ssh/id_ed25519 \
            -o IdentitiesOnly=yes \
            -o BatchMode=yes \
            -p "$PORT" \
            "$VPS_USER@$VPS_HOST" "bash -se" <<EOF
          set -euo pipefail
          cd "$APP_PATH"
          DOCKER_CONFIG="\$(mktemp -d)"
          trap 'rm -rf "\$DOCKER_CONFIG"' EXIT
          export DOCKER_CONFIG
          printf '%s\n' "$GHCR_TOKEN" | docker login ghcr.io -u "$GHCR_USER" --password-stdin
          docker compose -f docker-compose.deploy.yml --env-file .env.deploy pull
          docker compose -f docker-compose.deploy.yml --env-file .env.deploy up -d
          docker image prune -f
          docker compose -f docker-compose.deploy.yml --env-file .env.deploy ps
          EOF
