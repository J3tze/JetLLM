services:
  jetllm:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - jetllm-data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/jetllm.db
    restart: unless-stopped

volumes:
  jetllm-data:
